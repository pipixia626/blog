class Solution {
public:
    ListNode* ReverseList(ListNode* pHead) {
      ListNode*cur=pHead;
      ListNode*prev=nullptr;
        while(cur!=nullptr){
           ListNode*pNext=cur->next;
            cur->next=prev;
            prev=cur;
            cur=pNext;
            }
       return prev;
    }
};
